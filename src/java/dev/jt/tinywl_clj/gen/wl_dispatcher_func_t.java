// Generated by jextract

package dev.jt.tinywl_clj.gen;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef int (*wl_dispatcher_func_t)(const void *, void *, uint32_t, const struct wl_message {
 *     const char *name;
 *     const char *signature;
 *     const struct wl_interface **types;
 * } *, union wl_argument {
 *     int32_t i;
 *     uint32_t u;
 *     wl_fixed_t f;
 *     const char *s;
 *     struct wl_object *o;
 *     uint32_t n;
 *     struct wl_array *a;
 *     int32_t h;
 * } *)
 * }
 */
public class wl_dispatcher_func_t {

    wl_dispatcher_func_t() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        int apply(MemorySegment user_data, MemorySegment target, int opcode, MemorySegment msg, MemorySegment args);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
        C.C_INT,
        C.C_POINTER,
        C.C_POINTER,
        C.C_INT,
        C.C_POINTER,
        C.C_POINTER
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = C.upcallHandle(wl_dispatcher_func_t.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(wl_dispatcher_func_t.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static int invoke(MemorySegment funcPtr,MemorySegment user_data, MemorySegment target, int opcode, MemorySegment msg, MemorySegment args) {
        try {
            return (int) DOWN$MH.invokeExact(funcPtr, user_data, target, opcode, msg, args);
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}

